import { StrategyFormProps } from "@/types/backtest-types";

const BuyAndHoldForm: React.FC<StrategyFormProps> = ({
  onSubmit,
  isLoading,
}) => {
  return <div>Buy and hold form</div>;
};

export { BuyAndHoldForm };
