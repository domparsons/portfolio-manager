import { StrategyFormProps } from "@/types/backtest-types";

const LumpSumForm: React.FC<StrategyFormProps> = ({ onSubmit, isLoading }) => {
  return <div>Lump sum form</div>;
};

export { LumpSumForm };
